Port of `dablooms <https://github.com/bitly/dablooms>`_ in D.

*dablooms.cimpl* provides a direct port of the C code preserving the
interface while the other files provide D interfaces to that C
implementation.
